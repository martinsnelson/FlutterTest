import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Password generated lenght should be the size of the lenght parameter',
      () {
    //final password = PasswordGenerator.generate(length:16. lower: true, number:true, symbol:true);
    //expect(password.lenght, 16);
  });
}
